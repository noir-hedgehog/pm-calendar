
  # 职场黄历

  A workplace almanac for product managers, developers, testers, and project managers.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
